# WORKLOG — что мы делали

Новые записи сверху.

## 2026-09-23 — план: вход в кабинеты ФНС по сертификату testgost

**План.** Действие 11 `PLAN.md`: способом «по сертификату» дойти на lkfl2.nalog.ru и lkip2.nalog.ru до подписи
сертификатом 256 бит от testgost2012 и отказа сервера.

**Дальше.** Пробный прогон, затем тест с `STAND_ONLINE=1`.

## 2026-09-23 — план: настоящие КриптоПро CSP и плагин на стенде

**План.** Действие 10 `PLAN.md`: поставить присланные владельцем пакеты КриптоПро CSP 5.0.13800 и плагина
2.0.15700, подключить нативный хост плагина к браузеру стенда и проверить демо-страницу и testgost2012 на
включённом и невключённом сайте.

**Сделано.** Пакеты разобраны: плагину нужны `cprocsp-pki-cades` (скачан с публичной ссылки КриптоПро, пакет
плагина в том же архиве побайтно совпадает с присланным) и `lsb-cprocsp-rdr-64`, которого сначала не было; владелец
прислал его и `cprocsp-rdr-gui-gtk-64`. `scripts/setup-cryptopro-csp.ts` ставит пакеты, создаёт ключ в файловом
контейнере КриптоПро с сертификатом стендового УЦ, вносит адрес страницы в доверенные сайты плагина и делает копию
профиля стенда с нативным хостом КриптоПро. `servePages` принимает постоянный порт. Тест
`tests/stand/with-cryptopro-csp.spec.ts` (только с `STAND_CRYPTOPRO_CSP=1`): 3 из 3 зелёные вместе с
`STAND_ONLINE=1`; обычный `npm run test:stand` не изменился.

**Дальше.** Действие 7 — прогон владельцем.

## 2026-09-23 — план: наше расширение рядом с расширением КриптоПро

**План.** Действие 9 `PLAN.md`: расширение КриптоПро на стенде, тест, что на включённом сайте работает только наше,
а на невключённом — КриптоПро; testgost2012 с обоими.

**Сделано.** Пробный прогон: на включённом сайте `nmcades_plugin_api.js` КриптоПро не загружается, страница видит
нас; на невключённом загружается, страница пишет «Расширение загружено» и ждёт плагина КриптоПро.

Затем: расширение КриптоПро в `vendor-lock.json` и `setup-stand.ts` (общая `setupStoreExtension` с адаптером),
тест `with-cryptopro.spec.ts`; прогон testgost2012 с обоими расширениями зелёный.

**Дальше.** Действие 7 — прогон владельцем.

## 2026-09-23 — эксперимент с тестовым УЦ КриптоПро: выпуск и подпись

**План.** Действие 8 `PLAN.md`: эмуляция `X509Enrollment`, тест на стенде с сайтом УЦ, проверка подписей.

**Сделано.** Окно PIN и вход на токен вынесены в общий код (`token-login.ts`) для подписи, создания ключа и записи
сертификата. `src/page/objects/enrollment.ts` — объекты `X509Enrollment` страницы certsrv; `CadesError` теперь
несёт код HRESULT в `message`. Модульные тесты повторяют вызовы `async_code.js`; ответ УЦ для них —
`tests/fixtures/testgost-response.b64`. `verify_cms.py` берёт кривую из ключа и понимает 512 бит.
`tests/stand/testgost.spec.ts` (только с `STAND_ONLINE=1`): выпущены и установлены сертификаты на ключи 256 и
512 бит, подписи ими на демо-странице проверены. Версия 0.5.1.

**Дальше.** Действие 7 — прогон владельцем.

## 2026-09-23 — план эксперимента с тестовым УЦ КриптоПро

**План.** Поручение владельца: выпуск сертификата на testgost2012.cryptopro.ru через расширение и подпись им на
демо-странице. Записать действие 8 в `PLAN.md`, разобрать страницу запроса.

**Сделано.** Разобраны `certsrv/certrqma.asp` и `async_code.js` (какие объекты и методы вызываются); стенд с
выходом в сеть через прокси окружения открывает сайт. План записан.

**Дальше.** Эмуляция `X509Enrollment`, тест на стенде с сайтом УЦ.

## 2026-09-23 — этап 5: инструкция, чек-лист, сверка полей

**План.** Действия 5–6 `PLAN.md`.

**Сделано.** `docs/MANUAL-CHECK.md` — установка, ключ на токене, чек-лист демо-страницы, сверка полей.
`tools/dump-fields.js` (дамп полей из консоли DevTools) и `scripts/compare-fields.ts` (сравнение двух дампов);
тест стенда прогоняет дамп на демо-странице, модульный — сравнение. Ссылки на Рутокен и Chrome Web Store
открываются (проверено 2026-09-23); разделы ra.rutoken.ru названы по тексту сайта, путь целиком не пройден.

**Дальше.** Действие 7 — прогон владельцем; пауза.

## 2026-09-23 — этап 5: архив сборки

**План.** Действие 4 `PLAN.md`.

**Сделано.** `scripts/package.ts` и `npm run package`: ZIP с папкой расширения, собранный своим кодом на
`node:zlib` (без новой зависимости), проверен в модульном тесте независимым `zipfile` Python. CI выкладывает
архив артефактом.

**Дальше.** Действие 5 — инструкция и чек-лист.

## 2026-09-23 — этап 5: включение по сайтам

**План.** Действия 1–3 `PLAN.md`.

**Сделано.** Статический content script убран; `src/extension/` — список сайтов (`sites.ts`), фоновый скрипт,
окно кнопки, страница настроек; сборка кладёт их в `dist/extension/`. Добавлен dev-пакет `@types/chrome` (типы для
`src/extension/`, отдельный `tsconfig.json`). Модульные тесты списка (`tests/unit/sites.test.ts`), тесты стенда
`tests/stand/sites.spec.ts`; тесты демо-страницы теперь сначала включают сайт.

**Дальше.** Действие 4 — архив сборки для владельца.

## 2026-09-23 — план этапа 5

**План.** Владелец принял этап 4 и включение по сайтам. Записать план этапа 5.

**Сделано.** `docs/PLAN.md` переписан под этап 5, в `ROADMAP.md` этап 5 дополнен включением по сайтам и сверкой
полей. Версия поднята до `0.5.0`.

**Дальше.** Действия 1–2 — включение по сайтам.

## 2026-09-23 — этап 4 выполнен

**План.** Действие 5 `PLAN.md`: подпись на демо-странице стенда.

**Сделано.** Пять тестов в `tests/stand/demo-page.spec.ts`: присоединённая подпись, отсоединённая с «Проверять
подписанта», неверный PIN и повторный запрос PIN на каждую подпись, отмена, UTF-16LE по умолчанию. Все подписи
проверены `verify_cms.py`. Первый прогон упал: методы Рутокен Плагина возвращают thenable без `.catch()`
(`JOURNAL.md`); интерфейс `RutokenPlugin` теперь объявляет `PromiseLike`, чтобы компилятор ловил это. Снимки окна
PIN и готовой подписи: `/mnt/project-files/stage4/`.

**Дальше.** Пауза на проверку этапа 4 владельцем. Потом — включение расширения по сайтам (решение после этапа 4
в `PLAN.md`) и этап 5.

## 2026-09-23 — этап 4: окно PIN и объекты подписи

**План.** Действия 2–4 `PLAN.md`.

**Сделано.** `src/page/pin-dialog.ts` (окно в теневом DOM), `src/page/signing.ts` (PIN, `login`, `sign`,
`logout`), объекты `CPSigner`, `CPAttribute`, `CPAttributes`, `CadesSignedData`. `Session` теперь несёт origin
страницы и фабрику окна PIN, чтобы модульные тесты подставляли своё окно. Общие подделки плагина и окна вынесены
в `tests/unit/fakes.ts`. Модульные тесты: PIN, повтор после неверного PIN, отмена, блокировка, `logout` при
ошибке `sign`, UTF-16LE по умолчанию, PKCS#7 без атрибутов, отказы. На стенде ещё не проверено.

**Дальше.** Действие 5 — подпись на демо-странице стенда с проверкой `verify_cms.py`.

## 2026-09-23 — план этапа 4

**План.** Владелец принял этап 3; реальные поля сверим с КриптоПро на финальном этапе. Записать план этапа 4.

**Сделано.** `docs/PLAN.md` переписан под этап 4: независимый проверяльщик CMS, окно PIN, `CPSigner`,
`CPAttribute`, `CadesSignedData`, тесты подписи на демо-странице. Версия поднята до `0.4.0`.

**Дальше.** Действие 1 — проверяльщик.

## 2026-09-23 — этап 3 выполнен

**План.** Действия 1–5 `PLAN.md` этапа 3.

**Сделано.** Сертификат стенда с правдоподобным субъектом и сроком действия ключа; `src/page/asn1.ts`, `x509.ts`,
`dn.ts`, `sha1.ts`, `token.ts`; объекты `Store`, `Certificates`, `Certificate` и заглушка `CCspInformation`
(сверх плана, причина в `JOURNAL.md`); копии сертификатов стенда в `tests/fixtures/` для модульных тестов; тест
карточки на демо-странице.

**Сбой.** Коммит `cf81111` уронил CI на пять минут: модульный тест читал `stand/user.pem` в теле
`describe.skipIf`, а тело выполняется и при пропуске, стенда же на этом шаге CI ещё нет. Исправлено следующим
коммитом `c1868ec` — тесты берут копию сертификата из `tests/fixtures/`. Все последующие прогоны зелёные.

**Дальше.** Пауза на проверку этапа 3 владельцем, затем план этапа 4 (подпись CAdES-BES).

## 2026-09-23 — план этапа 3

**План.** Владелец принял этап 2 и подтвердил выбор версий, которые мы сообщаем сайтам. Записать план этапа 3.

**Сделано.** `docs/PLAN.md` переписан под этап 3: сертификат стенда с правдоподобным субъектом, разбор DER, `Store`,
`Certificates`, `Certificate`, тест карточки на демо-странице. Там же — выбранные по умолчанию значения и форматы,
которые ещё надо сверить с настоящим КриптоПро. Версия поднята до `0.3.0`.

**Дальше.** Действие 1 — сертификат стенда.

## 2026-09-23 — этап 2 выполнен

**План.** Действия 1–6 `PLAN.md` этапа 2.

**Сделано.** `scripts/build.ts` (esbuild, манифест MV3), `src/page/` (объект `window.cadesplugin`, загрузка
Рутокен Плагина, `CAdESCOM.About`), `scripts/gen-constants.ts` (константы из `cadesplugin_api.js`), файлы
демо-страницы в `vendor-lock.json`, тесты модульные и на стенде. Отклонений от плана нет; что узнали — в
`JOURNAL.md`.

**Дальше.** Пауза на проверку этапа 2 владельцем, затем план этапа 3 (сертификаты).

## 2026-09-23 — план этапа 2

**План.** Владелец принял этап 1 и сам удалил старую ветку. Записать план этапа 2.

**Сделано.** `docs/PLAN.md` переписан под этап 2: сборка расширения, объект `window.cadesplugin`, загрузка
Рутокен Плагина, `CAdESCOM.About`, демо-страница на стенде, тесты. Выбранные по умолчанию значения (какие версии
показывать сайту, когда запускать загрузку) — там же. Файлы демо-страницы скачиваются, хеши сняты.

**Дальше.** Действие 1 — сборка расширения.

## 2026-09-23 — CI, этап 1 закрыт

**План.** Действие 6 `PLAN.md` — CI.

**Сделано.** `.github/workflows/ci.yml`: `npm ci`, `npm run check`, установка Chromium, `npm run stand`,
`npm test`, `npm run test:stand`. Первый прогон зелёный; по логам стенд собран на раннере с нуля и дымовой тест
реально прошёл. Экшены переведены на v6 из-за предупреждения о Node 20, второй прогон тоже зелёный.

**Дальше.** Пауза на проверку этапа 1 владельцем, затем план этапа 2 (каркас расширения, демо-страница пишет
«Плагин загружен»).

## 2026-09-23 — стенд собран, дымовой тест зелёный

**План.** Действия 3–5 `PLAN.md`: сборка стенда, ключ и сертификат на токене, дымовой тест.

**Сделано.** `scripts/setup-stand.ts`, `scripts/provision-token.ts`, `tests/tools/gost_ca.py`,
`tests/stand/harness.ts`, `tests/stand/smoke.spec.ts`, конфиги Playwright и Vitest. Отклонение от плана:
плагин и fake Рутокен подключены симлинками в `$HOME/.mozilla/plugins`, а не подменой файла в каталоге плагина.
Причины и установленные порядки байт ГОСТ — в `JOURNAL.md`.

**Дальше.** Действие 6 — CI.

## 2026-09-23 — начало этапа 1

**План.** Владелец подтвердил дорожную карту и снял требование повторять объект подписи КриптоПро (критерий —
проверяемость, см. `JOURNAL.md`). Записать решение, затем действие 1 из `PLAN.md` — каркас Node-проекта.

**Сделано.** Решение записано в `CLAUDE.md`, `ANALYSIS.md`, `JOURNAL.md`. Ветку удалить не удалось (прокси), это в
`STATUS.md`.

**Дальше.** Действие 1 `PLAN.md`.

## 2026-09-23 — правила, анализ, дорожная карта

**План.** Поставить правила `coding-rules`, перенести анализ в `main`, по ответам владельца составить дорожную карту
и план первого этапа.

**Сделано.**

- `AGENTS.md` и `CLAUDE.md` из `coding-rules`; в `CLAUDE.md` — решения владельца.
- `docs/ANALYSIS.md`: анализ подхода, дополнен стендом без железа и разбором демо-страницы.
- `docs/ROADMAP.md`, `docs/PLAN.md` (этап 1 — стенд), `docs/STATUS.md`, `docs/JOURNAL.md`, `HANDOFF.md`.
- Проверена доступность вендорских файлов стенда, хеши записаны в `JOURNAL.md`.

**Дальше.** Подтверждение дорожной карты владельцем, затем действие 1 из `PLAN.md`.
