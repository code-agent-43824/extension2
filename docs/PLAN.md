# PLAN — этап 5: проверка владельцем на живом Рутокене

Начат 2026-09-23 после проверки этапа 4 владельцем.

Цель этапа — из [`ROADMAP.md`](ROADMAP.md): владелец ставит расширение в настоящий Chrome с настоящим Рутокеном и
проходит демо-страницу КриптоПро по чек-листу. До этого, по решению владельца от 2026-09-23, расширение
включается только на выбранных сайтах: без этого любой сайт может вызвать подпись, а настоящий КриптоПро
перехватывается везде (риски 7 и 9 в [`ANALYSIS.md`](ANALYSIS.md)).

Обозначения: `[ ]` не начато, `[~]` в работе, `[x]` сделано, `[!]` не получилось (причина в `JOURNAL.md`).

## Действия

- [x] **1. Включение по сайтам.** Статического content script больше нет. Список включённых сайтов хранится в
  `chrome.storage.local`; фоновый скрипт регистрирует `page.js` (`chrome.scripting.registerContentScripts`, мир
  `MAIN`, `document_start`) только для них. Доступ к сайту — необязательное разрешение (`optional_host_permissions`):
  Chrome спрашивает его, когда пользователь включает сайт, и при установке расширению не нужен доступ ко всем
  сайтам. Отозвал доступ в настройках Chrome — сайт выключается.
- [x] **2. Окно на кнопке расширения и страница настроек.** На кнопке: адрес текущего сайта и переключатель
  «Включено на этом сайте», после переключения вкладка перезагружается. В настройках: список включённых сайтов с
  кнопкой «Выключить» и поле для добавления адреса.
- [x] **3. Тесты.** Модульные — на логику списка и регистрации с поддельным `chrome`. На стенде: сайт не включён —
  страница работает без нас; включён через настройки — демо-страница подписывает; выключен — снова без нас.
  Существующие тесты демо-страницы сначала включают сайт. Окно кнопки на стенде открывается во вкладке: открыть
  настоящее всплывающее окно тест не может (`JOURNAL.md`).
- [x] **4. Сборка для владельца.** Архив `dist/cryptopro-via-rutoken-<версия>.zip` из `npm run package`; CI
  выкладывает его артефактом `cryptopro-via-rutoken` каждого прогона.
- [x] **5. Инструкция и чек-лист.** Что поставить (Рутокен Плагин, Адаптер из Chrome Web Store, наше расширение),
  как подготовить на Рутокене ключ ГОСТ и сертификат в PKCS#11, что пройти на демо-странице и что прислать —
  [`MANUAL-CHECK.md`](MANUAL-CHECK.md).
- [x] **6. Сверка полей с КриптоПро.** Вместо отдельной страницы — `tools/dump-fields.js`: его вставляют в консоль
  DevTools на демо-странице, он выводит поля «КриптоПро» и сертификатов в JSON; `scripts/compare-fields.ts`
  сравнивает дамп с КриптоПро и с нами. Отдельную страницу пришлось бы где-то размещать, а демо-страница уже
  загружает `cadesplugin` в обоих случаях. Подпись скрипт не делает: формат подписи не критерий (решение
  владельца), её переводы строк видны и в подписи с демо-страницы.
- [x] **8. Эксперимент владельца: сертификат с тестового УЦ КриптоПро.** Поручение владельца 2026-09-23: на
  <https://testgost2012.cryptopro.ru/> через наше расширение создать ключ и запрос, выпустить сертификат и
  установить его на токен, затем подписать им на демо-странице и проверить подпись. Для этого эмулируются объекты
  `X509Enrollment` страницы запроса (`certsrv/certrqma.asp` и её `async_code.js`): `CCspInformations`,
  `CX509PrivateKey`, `CX509CertificateRequestPkcs10`, `CX500DistinguishedName`, `CObjectId(s)`, расширения
  `KeyUsage` и `EnhancedKeyUsage`, `CX509Enrollment` (`CreateRequest`, `InstallResponse`). Ключ создаёт
  `generateKeyPair`, запрос — `createPkcs10`, сертификат ставит `importCertificate`; всё после PIN в нашем окне.
  Проверка на стенде — отдельный тест с выходом в сеть к одному этому сайту, не в CI: он зависит от чужого сервиса
  (`STAND_ONLINE=1`, `tests/stand/testgost.spec.ts`). Проверяльщик подписей научен ГОСТ 512 бит. Результат —
  `JOURNAL.md`.
- [x] **9. Рядом с расширением КриптоПро.** Поручение владельца 2026-09-23: поставить расширение КриптоПро ЭЦП
  Browser plug-in рядом с нашим и посмотреть обе страницы (демо и testgost2012); на включённом у нас сайте
  КриптоПро должно молчать. Его расширение (Web Store `pfhgbfnnjiafkhfdkmpiflachepdcjod`, версия 1.3.17) само в
  страницу ничего не ставит: `nmcades_plugin_api.js` грузит `cadesplugin_api.js` сайта, а тот выходит раньше, если
  `window.cadesplugin` уже наш. Проверка — тест стенда с обоими расширениями; расширение КриптоПро берётся в
  `vendor/` с проверкой SHA-256, как адаптер Рутокена. Самого КриптоПро CSP и плагина на стенде нет.
  Результат — `JOURNAL.md`; тест `tests/stand/with-cryptopro.spec.ts`.
- [x] **10. Рядом с настоящими КриптоПро CSP и плагином.** Поручение владельца 2026-09-23: поставить на стенд
  КриптоПро CSP 5.0.13800 и ЭЦП Browser plug-in 2.0.15700 (пакеты прислал владелец) и снова проверить обе страницы
  на включённом и невключённом у нас сайте. Пакеты не коммитятся: это лицензионные файлы КриптоПро, стенд с ними —
  локальный опыт (`scripts/setup-cryptopro-csp.ts`, тест `tests/stand/with-cryptopro-csp.spec.ts` только с
  `STAND_CRYPTOPRO_CSP=1`). Фейковый Рутокен — PKCS#11-библиотека, CSP ищет токены через PC/SC и его не видит,
  поэтому для подписи через КриптоПро ключ лежит в файловом контейнере КриптоПро. Результат — `JOURNAL.md`.
- [x] **11. Вход в личные кабинеты ФНС по сертификату testgost.** Поручение владельца 2026-09-23: с сертификатом
  ГОСТ 256 бит от testgost2012 войти способом «по сертификату» (он требует расширение и плагин КриптоПро) в кабинеты
  физлица (lkfl2.nalog.ru) и ИП (lkip2.nalog.ru) через наше расширение. Войти не выйдет — сертификат не от
  аккредитованного УЦ; успех — пройти предварительные проверки страницы, дойти до подписи и получить от сервера отказ
  в сертификате. Способ «Рутокен ЭЦП 3.0» — только для сравнения, JaCarta не трогаем (эмуляции нет), кабинет юрлица —
  тоже (нужен ГОСТ TLS). Токен — копия HOME стенда, тест `tests/stand/nalog.spec.ts` (`STAND_ONLINE=1`). Результат — `JOURNAL.md`.
- [x] **12. Хранилище корневых сертификатов в настройках.** Поручение владельца 2026-09-23: на странице настроек
  расширения — встроенное хранилище корневых сертификатов, карточка на каждый сертификат. Сертификат включается и
  выключается по отдельности, все сразу — тоже; хранилище целиком включается и выключается. Сертификат можно
  удалить и добавить из файла PEM или DER. Заранее хранилище заполнено корневыми сертификатами из пакета
  КриптоПро `lsb-cprocsp-ca-certs` 5.0.13800 (каталог `tmpcerts/root`, который КриптоПро ставит в хранилище
  корневых). С Рутокен Плагином и токеном хранилище пока никак не связано: сайты его не видят. Код —
  `src/extension/roots.ts` и `roots-view.ts`, сертификаты — `builtin-roots.ts` из `scripts/import-roots.ts`, тест
  `tests/stand/roots.spec.ts`.
- [x] **13. Хранилище корневых — сайтам.** Поручение владельца 2026-09-23: сайты, которые смотрят корневые
  сертификаты через интерфейс плагина КриптоПро, видят хранилище из действия 12; по умолчанию оно включено и
  заполнено сертификатами из пакета КриптоПро. С Рутокен Плагином и токеном его по-прежнему не связываем. Проверка —
  шаг checkCA проверки условий на lkip2.nalog.ru без обхода (`tests/stand/nalog.spec.ts`). Как у настоящего
  плагина 2.0.15700 (стенд, `JOURNAL.md`): `Open` хранилища `Root` в любом расположении, имя без учёта регистра,
  `HasPrivateKey()` — `false`, `PrivateKey` — ошибка `0x80092004`. Страница получает включённые сертификаты от
  скрипта расширения в изолированном мире: у скрипта страницы нет доступа к `chrome.storage`. Код —
  `src/page/roots.ts` и `src/extension/roots-bridge.ts`.
- [x] **14. Вход по сертификату на других сайтах.** Поручение владельца 2026-09-23: с теми же сертификатами
  testgost по порядку СФР, Честный знак, ЕИС Закупки, ГИС ЖКХ, Сбербанк-АСТ, Росэлторг. На каждом найти вход по
  сертификату, убедиться, что он требует плагин КриптоПро, и пройти его через наше расширение до отказа в
  сертификате. Мелочи исправлять по ходу, крупное — сначала согласовать с владельцем; о функциях плагина, которые
  раньше не встречались, — отчёт. Из окружения открылся только Честный знак (`tests/stand/crpt.spec.ts`);
  остальные разобраны по копиям web.archive.org, код Сбербанк-АСТ и Росэлторга прогнан на стенде без сети.
  Исправлено по ходу: `Find` по свойству, назначению ключа и сроку, `PrivateKey.CachePin`, порядок вызовов
  хранилища, сообщение «Рутокен Плагин не установлен». Отчёт — `JOURNAL.md` (2026-09-24).
- [x] **15. Подпись хеша и XMLDSig.** Решение владельца 2026-09-24 по отчёту действия 14: подпись хеша нужна
  обязательно, XMLDSig — если найдётся подходящая JavaScript-библиотека. `CAdESCOM.HashedData` (`Algorithm`,
  `DataEncoding`, `Hash`, `SetHashValue`, `Value`) и `CadesSignedData.SignHash` — по поведению настоящего плагина
  2.0.15700, снятому через `nmcades`; хеш считает Рутокен Плагин (`digest`), подписывает его `sign` с
  `DATA_FORMAT_HASH`. Проверка — `verify_cms.py` и сценарии Честного знака, Сбербанк-АСТ и `crypto-pro` на стенде.
  XMLDSig (`CAdESCOM.SignedXML`) — после выбора библиотеки, с отчётом владельцу. Подпись хеша сделана
  (0.5.7, `tests/stand/hash-signing.spec.ts`, поведение — `JOURNAL.md` 2026-09-24). XMLDSig сделан (0.5.8): из
  библиотек подошла только канонизация `xmldsigjs`, остальное своё; `tests/stand/xml-signing.spec.ts`, проверяльщик
  `tests/tools/verify_xmldsig.py`.
- [x] **16. Проверка подписи внутри расширения.** Решение владельца 2026-09-24: проверять подписи в самом
  расширении, без Рутокен Плагина и токена, с цепочкой до нашего хранилища корневых. ГОСТ Р 34.10-2012 (256, 512)
  и 34.10-2001 со Стрибогом и ГОСТ Р 34.11-94 — библиотека `@li0ard/gost` (MIT, на `@noble/curves`), версия
  закреплена; выбор и сверка — `JOURNAL.md`. Сначала снять поведение `Verify*` с настоящего плагина 2.0.15700
  через `nmcades`: коды ошибок, недоверенная цепочка, отзыв, что заполняется в `Signers`. Затем
  `CadesSignedData.VerifyCades` и `VerifyHash` (разбор CMS, подписанные атрибуты, подпись, цепочка из
  сертификатов подписи и включённых корневых), `SignedXML.Verify`. Проверка — подписи нашего стенда, подписи
  настоящего КриптоПро и испорченные подписи; модульные тесты и стенд. Отзыв сертификатов не проверяем — решение
  владельца 2026-09-24 по карточке. Сделано (0.5.9): `src/page/gost.ts`, `cms.ts`, `chain.ts`,
  `objects/signers.ts`; хеш `HashedData` и `SignedXML` теперь тоже в странице; `tests/unit/verify.test.ts`,
  `tests/stand/verification.spec.ts`, подписи для тестов — `tests/fixtures/verify.json`.
- [x] **17. Релиз 1.0 для ручной проверки.** Поручение владельца 2026-09-24: «подготовь релиз 1.0 для ручных
  проверок». Версия 1.0.0; после неё правило версий из `CLAUDE.md` меняется на обычное semver. Выпуск в GitHub
  Releases делает CI (тег через прокси не отправить): после зелёной проверки на `main`, если тега `v<версия>` ещё
  нет, создаётся предварительный выпуск с архивом `cryptopro-via-rutoken-<версия>.zip` — предварительный, пока
  владелец не прошёл его на устройстве. Чек-лист `MANUAL-CHECK.md` расширяется на всё, что сделано: демо-страницы
  КриптоПро подписи файла, XML-подписи и проверки подписи (`verify.html`), выпуск сертификата, хранилище
  корневых, соседство с КриптоПро. Эти демо-страницы сначала проходятся на стенде: копии в `vendor/cryptopro/`
  по SHA-256, тесты в `tests/stand/demo-pages.spec.ts`. Мелочи, найденные по ходу, исправляются; `verify.html`
  вызывает `AdditionalStore` и `GetMsgType`, которых у нас нет, — их поведение снимается с плагина 2.0.15700.
  Сделано: версия 1.0.0, правило версий, чек-лист (пункты 9–15), `AdditionalStore` и `GetMsgType`, три
  демо-страницы на стенде. Задание выпуска в CI записать не дали без явного решения владельца (`AGENTS.md` §6);
  владелец согласился, задание `release` добавлено; выпуск `v1.0.0` (предварительный, с архивом) создан CI
  2026-09-24.
- [x] **18. Промежуточные и сторонние корневые сертификаты.** Поручение владельца 2026-09-24: хранилище
  промежуточных сертификатов по тому же принципу, что и хранилище корневых, без связи с Рутокеном и Рутокен
  Плагином; промежуточные и сторонние корневые — на отдельной вкладке страницы настроек, чтобы не путать с
  настоящими корневыми. Причина — проверка подписи: демо-страница и многие сайты кладут в подпись только
  сертификат подписанта, а цепочку до корневого мы строили только из сертификатов подписи. Вкладка «Корневые» —
  встроенные корневые из пакета КриптоПро; вкладка «Промежуточные и сторонние корневые» — всё, что пользователь
  добавляет из файла: самоподписанный сертификат считается корневым, остальные — промежуточными. Сайты видят
  включённые корневые обеих вкладок как хранилище `Root`, промежуточные — как `CA`; проверка подписи строит
  цепочку и через промежуточные. Добавленные раньше в хранилище корневых невстроенные сертификаты при первом
  открытии переезжают на вторую вкладку, включённость сохраняется; встроенный, добавленный из файла, возвращается
  на первую. Сделано (1.1.0): `src/extension/roots.ts`, `roots-view.ts`, `options.html`, хранилище `CA` в
  `src/page/objects/store.ts`, промежуточные в `signed-data.ts`; тесты `tests/unit/roots.test.ts`,
  `tests/stand/roots.spec.ts`.
- [x] **19. `CAdESCOM.CadesTools` на демо-странице.** Владелец 2026-09-24 нашёл на странице расширений Chrome ошибку
  с демо-страницы: её `async_code.js` при загрузке зовёт `CAdESCOM.CadesTools.PackageManager()` (узнаёт плагин для
  Android), у нас объекта нет — необработанный отказ из page.js. Поведение настоящего плагина 2.0.15700 снять через
  `nmcades`, объект добавить; на стенде проверить, что демо-страницы не дают ошибок страницы. Сделано (1.1.1):
  `src/page/objects/cades-tools.ts`, тест стенда «raises no errors in the page» в `demo-page.spec.ts`.
- [x] **20. Расширенная проверка валидности сертификата и `Store.Add`.** Поручение владельца 2026-09-24: галочка
  в настройках «Расширенная проверка валидности сертификата», по умолчанию выключена — тогда `Certificate.IsValid()`
  как раньше, только сроки. Включена — `IsValid()` строит цепочку до корневых из хранилищ расширения, как КриптоПро:
  `Result`, `ValidationCertificates`, `ErrorStatuses` (порядок и флаги — с плагина 2.0.15700, `JOURNAL.md`); что
  сайты, ищущие только действительные сертификаты, покажут меньше, владелец принял. `Store.Add` — чтобы страница
  (ссылка демо-страницы «Установить корневой сертификат тестового УЦ») могла добавить сертификат: в `Root` и `CA` он
  попадает на вкладку «Промежуточные и сторонние корневые». Корневой (добавление в `Root` или самоподписанный) — после
  подтверждения в окне расширения, не в странице: страница не должна уметь нажать за пользователя; настоящий плагин в
  `Root` тоже ждёт подтверждения. Версия 1.2.0. Сделано (1.2.0): `chain.ts` `validationChain`, `IsValid()` и
  создаваемый `CAdESCOM.Certificate` с `Import` в `objects/certificate.ts`, `Store.Add` в `objects/store.ts`, путь
  через `roots-bridge.ts` и `install.ts`, окно `confirm.*`; тест стенда `validity.spec.ts`, пункт 16
  `MANUAL-CHECK.md`.
- [x] **21. Мелочи владельца после 1.2.0.** 2026-09-24: ссылка в окне кнопки — «Настройки» (там уже не только
  сайты); поле PIN не должно заполняться браузером. Версия 1.2.1. Сделано (1.2.1): `popup.html`; в `pin-dialog.ts`
  `autocomplete="one-time-code"` (значение `off` Chrome у поля пароля не выполняет) и отметки
  `data-lpignore`/`data-1p-ignore`/`data-bwignore` для менеджеров паролей; проверки в `sites.spec.ts` и
  `demo-pages.spec.ts`.
- [~] **22. Корневой УЦ при установке сертификата, как в Windows.** Решение владельца 2026-09-24. На Windows после
  «Установить этот сертификат» на странице УЦ система спрашивает, добавить ли корневой из ответа в доверенные; если
  нет — `0x800B0109`, и certfnsh.asp пишет «Данный ЦС не является доверенным» со ссылкой на корневой. Делаем так же
  отключаемой галочкой в настройках, по умолчанию включённой. `InstallResponse` строит цепочку выданного сертификата
  по хранилищам расширения и сертификатам ответа. Если она кончается недоверенным корневым, расширение в своём окне
  спрашивает «Да/Нет» с текстом, похожим на предупреждение Windows. «Да» — корневой на вторую вкладку, сертификат на
  токен. «Нет» — ничего не пишется, ответ `0x800B0109`, повторное «Установить этот сертификат» спросит снова.
  Промежуточные из ответа при включённой галочке кладутся на вторую вкладку молча, как Windows кладёт их в «CA». То
  же окно «Да/Нет» — и у `Store.Add` для корневого. Версия 1.3.0.
- [ ] **7. Прогон владельцем и исправления.** Подпись владельца проверяем `verify_cms.py` с сертификатом его УЦ.

## Значения по умолчанию, выбранные агентом

- **Что считается сайтом.** Origin: схема, хост и порт (`https://example.ru`); только `http` и `https`.
  Поддомены включаются отдельно.
- **Где правда о списке.** В `chrome.storage.local`; разрешение на доступ — условие, а не сам список. Так тест на
  стенде может выдать разрешение заранее, а включение и выключение пройдут через тот же код, что у пользователя.
- **Какие «криптопровайдеры» видит страница запроса** (действие 8). Два: тип 80 (ГОСТ Р 34.10-2012 256 бит) и
  тип 81 (512 бит, к имени добавлено «(ГОСТ Р 34.10-2012 512 бит)»), оба с именем из `About.CSPName`, как у
  сертификатов с токена. Ключ создаётся под выбранный тип; «ключ подписи» — `KEY_SPEC_SIGN`, иначе
  `KEY_SPEC_SIGN_AND_EXCHANGE`. Флаг «экспортируемый ключ» не выполняется: ключ Рутокена не экспортируется. Уже
  существующий ключ и хранилище компьютера отклоняются с понятной ошибкой.
- **На какой токен.** На единственный подключённый: сайт токен не называет, а угадывать при нескольких — значит
  рисковать записать не туда. При нескольких — ошибка «оставьте один».
- **Что ставит `InstallResponse`.** Только сертификат пользователя, после проверки, что на токене есть его ключ
  (иначе сертификат снимается и ошибка `0x80092004`, которую страница УЦ понимает). Сертификаты УЦ из ответа не
  пишутся: для подписи они не нужны, а хранилища корневых у нас нет. Повторная установка того же ответа — не ошибка.
- **Хранилище корневых** (действие 12). Лежит в `chrome.storage.local`; при первом открытии заполняется
  встроенными сертификатами, все включены, хранилище включено. Удалённый встроенный сертификат возвращается только
  добавлением из файла. Промежуточные сертификаты пакета (`tmpcerts/ca`) не берутся: это хранилище корневых.
  Добавить можно любой сертификат X.509; файл — DER, PEM (один или несколько сертификатов) или base64 без
  заголовков, как сохраняет КриптоПро; повтор по отпечатку SHA-1 не добавляется.
- **Фреймы.** Скрипт внедряется и во фреймы включённого сайта (`allFrames`), но только если адрес самого фрейма
  тоже включён.
