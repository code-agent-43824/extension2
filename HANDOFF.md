# HANDOFF — что не закончено

Обновлено: 2026-09-24.

Идёт этап 5 — [`docs/PLAN.md`](docs/PLAN.md). Действия 1–6 и 8–18 (тестовый УЦ КриптоПро, расширение КриптоПро рядом,
настоящие CSP и плагин КриптоПро на стенде, кабинеты ФНС, хранилище корневых, вход по сертификату на других
сайтах, подпись хеша и XMLDSig, проверка подписи, выпуск 1.0, промежуточные сертификаты) сделаны; ручной прогон
владельцем (действие 7) идёт: вход на lkfl2, демо-страница и testgost2012 на его Рутокене прошли.

Незаконченной работы нет. Действие 18 сделано (1.1.0): вторая вкладка хранилища — промежуточные и сторонние
корневые. Действие 19 (1.1.1): `CAdESCOM.CadesTools` для демо-страниц. Действие 20 (1.2.0): расширенная проверка
валидности и `Store.Add`; выпуск `v1.2.0` создаёт CI. Стенд, собранный до 1.0.0, нужно дополнить:
`node scripts/fetch-vendor.ts` скачает три новые демо-страницы.

**Дальше:** действие 7 — владелец пришлёт подписи квалифицированным сертификатом (нашу и КриптоПро, присоединённую и
отсоединённую, с `cades_bes_sample.html`), дампы `dump-fields.js` и сертификат УЦ: проверить подписи `verify_cms.py` и
нашей проверкой (сертификат УЦ — на вторую вкладку), сравнить поля `scripts/compare-fields.ts`. После его проверки снять с выпуска отметку «предварительный» может только он.

Попутно найдено: `STAND_ONLINE=1` `nalog.spec.ts` (lkfl2) сейчас падает — в списке страницы только сертификат
стенда `Stand User`, сертификата testgost физлица, выпущенного в `beforeAll`, нет; не разбиралось.
