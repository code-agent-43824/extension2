# HANDOFF — что не закончено

Обновлено: 2026-09-23.

Идёт этап 5 — [`docs/PLAN.md`](docs/PLAN.md). Действия 1–6 и 8 (эксперимент с тестовым УЦ КриптоПро) сделаны; ручной
прогон владельцем (действие 7) отложен им.

**В работе — действие 10** (настоящие КриптоПро CSP и плагин на стенде). Присланные владельцем пакеты разобраны в
каталоге сессии (не в репозитории); `cprocsp-pki-cades` скачан с
`https://cryptopro.ru/sites/default/files/products/cades/current_release_2_0/cades-linux-amd64.tar.gz`. Не хватает
`lsb-cprocsp-rdr-64` (`librdrsup.so.4`, `libcapi10.so.4`, `libcpui.so.4`), у владельца он запрошен. Следующий шаг:
получить пакет, поставить всё `dpkg -i`, прописать `ru.cryptopro.nmcades` в `NativeMessagingHosts` профиля стенда,
создать ключ в файловом контейнере КриптоПро и прогнать `with-cryptopro.spec.ts` и `testgost.spec.ts`.

**Потом:** действие 7 — когда владелец пришлёт подписи и сертификат УЦ, проверить их `verify_cms.py`, разобрать
его заметки и дампы полей. Нового кода до его ответа не начато.
